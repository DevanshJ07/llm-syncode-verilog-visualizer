# Copy to .env.local before running the frontend.
# Dedicated App Router proxies (/api/generate, /api/import/bundle,
# /api/imported-experiment/[id]) and rewrites use BACKEND_URL.
# Do not hardcode a machine-specific path; change this per environment.

BACKEND_URL=http://127.0.0.1:8000
